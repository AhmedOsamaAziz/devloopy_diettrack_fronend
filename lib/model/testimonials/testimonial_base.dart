abstract class TestimonialBase {
  String title;
  String description;
  String videoUrl;

  TestimonialBase({
    required this.title,
    required this.description,
    required this.videoUrl,
  });
}
